import React from 'react';

import classes from './Card.module.css';

const Card = () => {
  return (
    <div>
      <h1 className={classes.heading}>THE GRAYSCALE CHESS BOARD</h1>
    </div>
  );
};

export default Card;
